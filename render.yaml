services:
  - type: web
    name: dermavision-api
    env: python
    rootDir: system_training
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT